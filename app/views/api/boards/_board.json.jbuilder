json.extract! board, :id, :user_id, :name, :description

# json.board do
#     json.extract! board, :id, :user_id, :name, :description
# end

# json.pins do
#     json.extract! pins, :id, :user_id, :title, :description, :photoUrl, :pinUrl
# end